let words = ['fries', 'chicken nuggets', 'handburger', 'hot dog', 'icecream', 'fried chicken', 'donuts', 'cup cake', 'cake', 'pan cake', 'sauce', 'apple pie', 'macaroni', 'pasta', 
'nachos', 'pizza'];
let size = words.length;
let randomNum = Math.Floor(Math.random() * size);
let word = words[randomNum];

for (let i = 0; i < word; i++) {
    if (word == ' ') {
        
    }
}
