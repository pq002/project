const words = ['fries', 'chicken nuggets', 'handburger', 'hot dog', 'icecream', 'fried chicken', 'donuts', 'cup cake', 'cake', 'pan cake', 'sauce', 'apple pie', 'macaroni', 'pasta', 
'nachos', 'pizza'];
let size = words.length;
let randomNum = Math.floor(Math.random() * size);
let Word = words[randomNum];

for (let i = 0; i < words[i].length; i++) {
  word.innerHTML += '_ ';
}

guessButton.addEventListener('load', () => {
    
  });